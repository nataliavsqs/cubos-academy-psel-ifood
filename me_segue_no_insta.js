
function solucao(largura, altura) {
    if (largura > altura ){
        console.log ("PAISAGEM");
    } if ( largura < altura){
        console.log ("RETRATO");
    } if (largura === altura) {
        console.log ("QUADRADA");
    };
    
  }